trigger:
- main

pool:
 name: test-pool

stages:
- stage:
  jobs:
  - job: BuildPushImage
    steps:

    - task: Docker@2
      inputs:
        containerRegistry: 'DockerR'
        repository: 'azuretest'
        command: 'buildAndPush'
        Dockerfile: '**/Dockerfile'
